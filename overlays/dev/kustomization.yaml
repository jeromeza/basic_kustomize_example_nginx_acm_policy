apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: jerome-dev
resources:
  - ../../base
patches:
  - target:
      kind: Deployment
      name: nginx-server
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 2

